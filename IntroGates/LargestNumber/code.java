class Code
{
    int largestNumber(int n)
    {
        String answer = "";
        for(int i=0;i<n;i++)
        {
            answer = answer + "9";
        }
        return Integer.decode(answer);
    }
}
